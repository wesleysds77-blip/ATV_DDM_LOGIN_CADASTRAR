import { Image, Pressable, StyleSheet, Text, View, Button, Alert, TextInput } from 'react-native';


export default function App() {

  return (
    <View style={styles.container}>

      <Image
      source={{ uri: "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQ1gOYhj_HmxIQj42Xkn0acnB5mGVaGU9zZzOa7EgkZTg&s=10"}}
      style={styles.imagem}
      />

      <TextInput
        style={styles.input}          
        placeholder="Digite seu nome"
      />

      <TextInput
        style={styles.input}
        placeholder="Digite sua senha"
        secureTextEntry={true}
      />

      <View style={styles.botoes}> 
      <Button title="Logar" onPress={() => alert('Entrou na conta')} /> 
      </View>

      <Pressable
      onPress={() => {alert("É uma pena")}}
      >

      <Text style={styles.texto}>
      Esqueceu a sua senha?
      </Text>

      </Pressable>


    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#ecf0f1',
    padding: 16,
  },

  input: {
    width: '80%',
    borderWidth: 1,
    borderColor: '#ccc',
    padding: 10,
    margin: 10,
    borderRadius: 5,
    backgroundColor: '#fff',
  },

  botoes: {
    width: '80%',
    margin: 10,
    gap: 10,
  },
  texto: {
    color: 'blue',
    margin: 10,
    gap: 10,
  },
  imagem: {
 width: 160,
        height: 160,
        borderWidth: 2,
        margin: 20
  },

});
